(cons nil nil);here we are trying to create a list with head as the 
;first element(nil) and the rest of the statement as the list
;the problem is that we need to use ' and ()to make it work

;(cons 'nil '(nil)); the right way of creating a list