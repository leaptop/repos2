TabWidget создаёт сверху вкладки с картиночками прям как для нескольких документов в нотепад++
TableWidget просто матрицу сосздает из new QTableWidgetItem(QString("%1,%2").arg(i).arg(j));
ComboBox - выпадающий список по функциональности то же, что и QListWidget, но всё время показывает только один элемент, остальные "выпадают при нажатии"